import 'package:flutter/material.dart';

void main() {
  runApp(const MyTask());
}

class MyTask extends class MyWidget extends StatelessWidget {
  const MyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
} 